#!/usr/bin/env sh
set -eu
PATH="$HOME/.bun/bin:$HOME/.local/bin:$PATH"
export PATH
SOURCE=$0
while [ -L "$SOURCE" ]; do
  DIR=$(CDPATH='' cd -P -- "$(dirname -- "$SOURCE")" && pwd)
  TARGET=$(readlink "$SOURCE")
  case $TARGET in
    /*) SOURCE=$TARGET ;;
    *) SOURCE=$DIR/$TARGET ;;
  esac
done
DIR=$(CDPATH='' cd -P -- "$(dirname -- "$SOURCE")" && pwd)
if command -v bun >/dev/null 2>&1 && [ -r "$DIR/dist/pi-remote.js" ]; then
  exec bun "$DIR/dist/pi-remote.js" "$@"
fi
exec "$DIR/pi-remote.sh" "$@"
