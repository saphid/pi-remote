{
  "name": "pi-remote",
  "version": "1.0.0",
  "description": "Tiny SSH/tmux launcher for remote coding-agent sessions.",
  "bin": {
    "pi-remote": "dist/pi-remote.js",
    "pi-remote.sh": "pi-remote.sh"
  },
  "files": [
    "assets",
    "dist",
    "pi-remote",
    "pi-remote.sh",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
